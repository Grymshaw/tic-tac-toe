function scoreboard(players) {
    let p1,
        p2;

    function init() {
        p1 = players[0];
        p2 = players[1];
        render();
    }
    function render() {
        //TODO: change scoreboard to reflect each players' wins
    }
}
